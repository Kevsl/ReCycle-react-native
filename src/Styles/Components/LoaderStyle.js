import { StyleSheet } from 'react-native'

export const LoaderStyle = (StyleSheet.createstyle = StyleSheet.create({
    container: {
        width: '50%',
        marginHorizontal: '25%',
        marginVertical: 30,
    },
}))
